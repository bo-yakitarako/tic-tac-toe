import { describe, test } from 'bun:test';

describe('minMax', () => {
  test('縦横斜めの列数を数える', () => {});
  test('ノードのある状態のときの評価値を求める', () => {});
  test('全ての子ノードの評価値のminとmaxを求める', () => {});
  test('全ての子ノードのうち最大の評価値を求める', () => {});
  test('ある状態のときのCPUの最善手を決定する', () => {});
});
